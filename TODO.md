# TODO: Бургер-меню з фіксованим прозорим navbar (ЗАВЕРШЕНО ✅)

## Виконані кроки:
1. ✅ Створено TODO.md
2. ✅ Оновлено `src/components/styles/App.css`: 
   - Navbar: fixed top з прозорим фоном `rgba(18,18,18,0.95)` + blur(25px), z-index 10000
   - Nav-link: прозорий фон `rgba(255,255,255,0.08)` + blur(15px), border, padding, z-index 1001
   - Кнопки .theme-toggle, .language-toggle: аналогічний прозорий фон + blur, padding
   - Додано `padding-top: 90px` до main для уникнення перекриття
   - Адаптивні стилі для планшетів (992-1100px): прозорість на nav-link/кнопках
3. ✅ Збережено всі hover-ефекти (3D transform, shadow)
4. ✅ Фіксованість: navbar/nav-link/кнопки завжди на верху

## Результат:
Бургер-меню готове: фіксований navbar з прозорим фоном, nav-link та кнопки з трохи прозорим фоном, все скролиться під ним. Не перероблено nav-link логіку.

Відкрийте проект у браузері для перевірки (F5 або `npm start`).
